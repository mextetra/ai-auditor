# Azure AI Foundry Credentials
AZURE_INFERENCE_ENDPOINT="https://your-resource-name.openai.azure.com/"
AZURE_INFERENCE_CREDENTIAL="your-api-key"

# Governance Settings
GOVERNOR_STRICTNESS="high"  # Options: high, medium, low
ENABLE_AUDIT_LOGGING="true"
MAX_RESPONSE_LENGTH="2000"

# Audit Log Configuration
AUDIT_LOG_RETENTION_DAYS="90" # NIST requires retention
AUDIT_LOG_MAX_SIZE_MB="500"   # Log rotation threshold

# Rate Limiting (Per User)
RATE_LIMIT_HOURLY="100"
RATE_LIMIT_BURST="10"
